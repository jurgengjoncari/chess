let container = document.querySelector('[data-board]')

let board = [...Array(8)].map(e => Array(8))

console.log(board);

for (let i = 0; i < 8; i++) {
    let board = []
    let rank = i
    for (let j = 0; j < 8; j++) {
        let file = j
        
    }
}